@model IEnumerable<Pweb_kilme_.Models.dbModels.Quintum>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PrecioPorPersona)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Direccion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdImagenNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdRedesNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PrecioPorPersona)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Direccion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdImagenNavigation.IdImg)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdRedesNavigation.IdRedes)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdQuinta">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdQuinta">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdQuinta">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
